# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      debug_enabled:
        type: boolean
        description: 'Run the build with tmate debugging enabled'
        required: false
        default: false
      os:
        type: string
        description: 'Only run for specified OS'
        required: false
        default: "all"

name: R-CMD-check

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest,    r: 'release'}
          - {os: windows-latest,  r: 'release'}
          - {os: ubuntu-latest,   r: 'devel', http-user-agent: 'release'}
          - {os: ubuntu-latest,   r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v3
        if: |
          github.event_name != 'workflow_dispatch' ||
          (github.event_name == 'workflow_dispatch' &&
            (inputs.os == matrix.config.os || inputs.os == 'all'))

      - uses: r-lib/actions/setup-tinytex@v2
        if: |
          github.event_name != 'workflow_dispatch' ||
          (github.event_name == 'workflow_dispatch' &&
            (inputs.os == matrix.config.os || inputs.os == 'all'))
        name: Install additional LaTeX packages for building vignettes
        run: |
          tlmgr install grfext preprint

      - uses: r-lib/actions/setup-r@v2
        if: |
          github.event_name != 'workflow_dispatch' ||
          (github.event_name == 'workflow_dispatch' &&
            (inputs.os == matrix.config.os || inputs.os == 'all'))
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        if: |
          github.event_name != 'workflow_dispatch' ||
          (github.event_name == 'workflow_dispatch' &&
            (inputs.os == matrix.config.os || inputs.os == 'all'))
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - uses: r-lib/actions/check-r-package@v2
        if: |
          github.event_name != 'workflow_dispatch' ||
          (github.event_name == 'workflow_dispatch' &&
            (inputs.os == matrix.config.os || inputs.os == 'all'))
        with:
          upload-snapshots: true

      # Enable tmate debugging of manually-triggered workflows if the input option was provided
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: |
          github.event_name == 'workflow_dispatch' &&
          inputs.debug_enabled && (inputs.os == matrix.config.os || inputs.os == 'all') && failure()
        with:
          limit-access-to-actor: true
